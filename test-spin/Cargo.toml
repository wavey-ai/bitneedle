[package]
name = "test-spin"
version = "0.1.6"
edition = "2021"
license = "Apache-2.0"
publish = false

[[bin]]
name = "record-test"
path = "src/main.rs"

[dependencies]
anyhow = "1"
hex = "0.4"
serde_json = "1"

record-core = { path = "../record-core" }
record-descriptor = { path = "../record-descriptor" }
record-decode = { path = "../record-decode" }
encodec-rs = { path = "../../encodec-rs", features = ["ecdc"] }
