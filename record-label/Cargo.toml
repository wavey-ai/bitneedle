[package]
name = "record-label"
version = "0.1.6"
edition = "2021"
description = "Bitneedle label geometry and cutout authoring primitives"
repository = "https://github.com/wavey-ai/bitneedle"
readme = "README.md"
license-file = "LICENSE"

[dependencies]
anyhow = "1"
record-core = { path = "../record-core", version = "0.1.6" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
