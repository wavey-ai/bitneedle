__pycache__
.wrangler
.DS_Store
.secrets
cdp_api_key.json
node_modules/
.venv*/
target/
dist/
wasm/
.cache/
.wasm-pack-build.lock/
.cache-local/
.tmp-local/
*.local.ecdc
*.local.wav

# Local scratch (screenshots, debug scripts)
/tmp/

# Output that the examples and the fixture test write to the repository root.
# `cargo test -p test-spin -- --ignored write_a_fixture_record` writes
# fixture-record.png, a real pressed record, for driving the apps by hand.
/*.png
